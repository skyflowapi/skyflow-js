node_modules
*.log
build

release
build
dist
testing
coverage

# Generated declaration output only (tsc outDir). Anchored to repo root so it
# does not also ignore the core/types/ source folder.
/types
