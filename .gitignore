node_modules
*.log
build

release
build
dist
testing
coverage

# Generated declaration output only (tsc outDir). Anchored so it does not also
# ignore the core/types/ source folder. Each package emits into its own types/.
/types
packages/*/types
